# laravel-true-modular-health

Module-scoped health checks for `laravel-true-modular`: declare checks per module, aggregate runtime + architecture health via Spatie Laravel Health and `module:doctor`.
